import SwiftUI
import AVFoundation

struct ContentView: View {
    var body: some View {
        Waveform()
            .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
